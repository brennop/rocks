import javax.swing.*;

public class Asteroid extends Entity {
    public Asteroid(int x, int y, double rotation, int size, float speed, JPanel game) {
        super(x, y, rotation, size, speed, game);
    }
}
